#include <iostream>
using namespace std;

int main()
{
    int num,a,i,sum;
    cout<<"Enter a number :";
    cin>>num;
    i=num%10;
    while(num>10){
       if (a=num/10);
        num=a;
        sum=a+i;
        }cout<<"The sum of first digit and last digit of the number = "<<sum;
    return 0;
}
